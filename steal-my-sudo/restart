#!/bin/sh

set -e

if [ "$USER" != 'root' ]; then
	echo 'Please run as root'
	exit 1
fi

name=$(basename $(pwd))

podman stop $name

podman run --rm -d \
	--name=$name \
	--network=ctfprivate \
	$name

# --publish=4022:22 \
